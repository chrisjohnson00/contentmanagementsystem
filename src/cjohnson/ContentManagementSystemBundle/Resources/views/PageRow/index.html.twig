{% extends '::base.html.twig' %}

{% block body -%}
    <h1>PageRow list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Rank</th>
                <th>Createdon</th>
                <th>Modifiedon</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_pagerow_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.rank }}</td>
                <td>{% if entity.createdOn %}{{ entity.createdOn|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.modifiedOn %}{{ entity.modifiedOn|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_pagerow_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_pagerow_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_pagerow_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
