{% extends '::base.html.twig' %}

{% block body -%}
    <div>
        <ol class="breadcrumb">
            {% include 'cjohnsonContentManagementSystemBundle:Admin:breadcrumbDropdown.html.twig' %}
            <li><a href="{{ path('admin_page') }}">Pages</a></li>
            <li class="active">Page: {{ entity.name }}</li>
        </ol>
        <h1>Page edit</h1>
    </div>
    <div>
        {{ form(edit_form) }}
    </div>
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10"><a class="btn btn-default" href="{{ path('admin_page') }}">
                <span class="glyphicon glyphicon-arrow-left"></span> Back to the page list
            </a></div>
    </div>
    <div>
        {{ form(delete_form) }}
    </div>
{% endblock %}
