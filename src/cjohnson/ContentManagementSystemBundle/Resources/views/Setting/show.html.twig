{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Setting</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Bootstrapversion</th>
                <td>{{ entity.bootstrapVersion }}</td>
            </tr>
            <tr>
                <th>Bootswatchtemplate</th>
                <td>{{ entity.bootswatchTemplate }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('setting') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('setting_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
