{% extends '::base.html.twig' %}

{% block body -%}
    <ol class="breadcrumb">
        {% include 'cjohnsonContentManagementSystemBundle:Admin:breadcrumbDropdown.html.twig' %}
        <li><a href="{{ path('admin_image') }}">Images</a></li>
        <li class="active">Image</li>
    </ol>   <h1>Image edit</h1>
    <img class="img-responsive"
         src="{{ vich_uploader_asset(entity, 'imageFile')|imagine_filter('imagePreview') }}"/>
    {{ form(edit_form) }}

    <a class="btn btn-default" href="{{ path('admin_image') }}">
        <span class="glyphicon glyphicon-arrow-left"></span> Back to the list
    </a>
    {{ form(delete_form) }}
{% endblock %}