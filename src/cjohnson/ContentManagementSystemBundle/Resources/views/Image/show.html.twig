{% extends '::base.html.twig' %}

{% block body -%}
    <ol class="breadcrumb">
        {% include 'cjohnsonContentManagementSystemBundle:Admin:breadcrumbDropdown.html.twig' %}
        <li><a href="{{ path('admin_image') }}">Images</a></li>
        <li class="active">Image</li>
    </ol>
    <img class="img-responsive"
         src="{{ vich_uploader_asset(entity, 'imageFile') }}"/>

    <ul class="record_actions">
        <li>
            <a href="{{ path('admin_image') }}">
                Back to the list
            </a>
        </li>
        <li>
            <a href="{{ path('admin_image_edit', { 'id': entity.id }) }}">
                Edit
            </a>
        </li>
    </ul>
    {{ form(delete_form) }}{% endblock %}
